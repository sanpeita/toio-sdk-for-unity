# AGENTS.md

このファイルは、このワークスペースにおける Codex のサブエージェント運用ルールを定義する。

## サブエージェント運用ルール

- サブエージェントの活用は、原則として常設許可とする。
- ユーザーが `サブエージェント中止` と指示した場合、その時点でサブエージェント運用を停止する。
- メイン担当 Codex は、自身の判断で効率運用のためにサブエージェントを召喚してよい。
- サブエージェントは、探索・並列調査・分担実装・検証など、作業効率が明確に上がる場面で優先的に活用してよい。
- サブエージェント使用時は、役割に応じた通称を付けてよい。
- サブエージェントの通称は固定しすぎず、運用上アンマッチだと判明した場合は、その都度見直してよい。
- 重要判断、最終方針、最終統合、ユーザーへの最終説明責任はメイン担当 Codex が持つ。
- サブエージェントを使う際は、目的と担当範囲が重ならないよう整理すること。
- サブエージェントの利用が不要、過剰、またはかえって複雑化すると判断した場合は、メイン担当 Codex 単独で進めてよい。

## 実務運用の補足

- まずメイン担当 Codex が全体方針と直近のクリティカルパスを整理する。
- 次の一手がその結果待ちで止まる仕事は、原則としてサブエージェントに丸投げせず、メイン担当 Codex が自分で進める。
- サブエージェントには、具体的で独立した、境界の明確なタスクだけを渡す。
- 並列化する場合は、互いに担当ファイルや責務が衝突しないようにする。
- サブエージェントに任せた作業を、メイン担当 Codex が並行して重複実施しない。
- サブエージェント起動後、メイン担当 Codex は待機せず、別の非重複タスクを進める。
- 結果待ちで本当にブロックされる場合だけ、サブエージェントの完了待ちを行う。
- 実装系のサブエージェントには、担当範囲を明示し、他の担当の変更を勝手に巻き戻さないことを徹底する。
- 最後はメイン担当 Codex が全体整合を確認してからユーザーに返す。

## 推奨される使いどころ

- コードベース探索
- 複数候補の並列調査
- 独立した実装の分担
- テストや検証の並列実施
- リスク確認やレビュー補助
